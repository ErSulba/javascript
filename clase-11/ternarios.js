// ternarios
let miVariable
if (condition) {
  // hacer algo
  miVariable = "tal cosa"
} else {
  //sino haz esto
  miVariable = "otra cosa"
}

// (condicion) ? codigo a ejecutar si se cumple la condicion : codigo a ejecurar si no se cumple
1 > 0 ? console.log("se cumplio") : console.log("es mentira")

1 > 2 ? console.log("se cumplio") : console.log("es mentira")

let miVariable2 = true ? "se cumplio" : "no se cumplio"
console.log("🚀 ~ file: ternarios.js ~ line 16 ~ miVariable2", miVariable2)